@model IEnumerable<IPGManager.Models.Horario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HInicioIntervalo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HFimIntervalo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HFim)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HInicioIntervalo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HFimIntervalo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HFim)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdHorario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdHorario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdHorario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
